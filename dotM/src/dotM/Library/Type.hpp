#pragma once

#include <cstdint>

//using bit   = bool;
using byte  = std::uint8_t;
using word  = std::uint16_t;
using dword = std::uint32_t;
using qword = std::uint64_t;

using addr8_t  = byte;
using addr16_t = word;
using addr32_t = dword;
